<%= form_with(model: character, local: true) do |form| %>
  <% if character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(character.errors.count, "error") %> prohibited this character from being saved:</h2>

      <ul>
      <% character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :actor_name %>
    <%= form.text_field :actor_name %>
  </div>

  <div class="field">
    <%= form.label :character_name %>
    <%= form.text_field :character_name %>
  </div>

  <div class="field">
    <%= form.label :url_photo %>
    <%= form.text_field :url_photo %>
  </div>

  <div class="field">
    <%= form.label :serie_id %>
    <%= form.text_field :serie_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
